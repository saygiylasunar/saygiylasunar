{
  "name": "saygiylasunar-portfolio-v2",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "generate:og": "node scripts/generate-og.mjs",
    "generate:seo": "npm run generate:sitemap && npm run generate:og",
    "prebuild": "npm run check:content && npm run generate:seo",
    "check:content": "node scripts/check-content.mjs",
    "check:bundle": "node scripts/check-bundle-budget.mjs",
    "postbuild": "npm run check:bundle",
    "dev:seo": "npm run generate:seo && vite"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.5.1",
    "dompurify": "^3.4.15",
    "marked": "^18.0.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "vite": "^6.3.3",
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
